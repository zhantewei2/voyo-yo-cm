import { DirectiveBinding } from "vue/types/options";
import { VNode } from "vue";
export declare const TabDirective: {
    bind: (el: HTMLElement, binding: DirectiveBinding, vNode: VNode) => void;
};
